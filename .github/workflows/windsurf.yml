name: Windsurf IDE

on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Fetch all tags
        run: git fetch --tags

      - name: Get latest tag and set next tag
        id: version
        run: |
          # Get the latest tag matching v1.*
          latest_tag=$(git tag --list "v1.*" --sort=-v:refname | head -n 1)
          if [ -z "$latest_tag" ]; then
            next_tag="v1.0.0"
          else
            # Extract the version numbers
            IFS='.' read -r major minor patch <<<"${latest_tag#v}"
            patch=$((patch + 1))
            next_tag="v${major}.${minor}.${patch}"
          fi
          echo "Next tag: $next_tag"
          echo "TAG_NAME=$next_tag" >> $GITHUB_ENV

      - name: Configure Git
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

      - name: Create and push Git tag
        run: |
          git tag $TAG_NAME
          git push origin $TAG_NAME

      - name: Download Windsurf Installer
        run: |
          curl -L -o WindsurfUserSetup-x64-1.9577.43.exe \
          https://windsurf-stable.codeiumdata.com/win32-x64-user/stable/745a6c1ac471cc11f782a05d2c3ceacbc1de308f/WindsurfUserSetup-x64-1.9577.43.exe
      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG_NAME }}
          files: WindsurfUserSetup-x64-1.9577.43.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
